<?xml version="1.0" encoding="utf-8"?>
<!-- Pairs with slide_in_right.xml: the screen being left behind eases out
     quickly (shorter duration than the incoming screen's entrance, so the
     new content is what draws the eye) with a gentle fade + slight slide,
     using the same claude_ease curve as the entrance (see
     res/interpolator/) instead of the platform's accelerate_cubic — the
     app never mixes a separate "accelerate" feel into an exit, just a
     shorter duration than the enter side. -->
<set xmlns:android="http://schemas.android.com/apk/res/android">
    <translate
        android:fromXDelta="0%"
        android:toXDelta="-8%"
        android:duration="200"
        android:interpolator="@interpolator/claude_ease" />
    <alpha
        android:fromAlpha="1.0"
        android:toAlpha="0.0"
        android:duration="180"
        android:interpolator="@interpolator/claude_ease" />
</set>
